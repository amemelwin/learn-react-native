import { StyleSheet } from "react-native";
import Colors from "../consts/Colors";
const userListStyles = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor:Colors.light_purpel,
    },
    userList:{
        paddingVertical:10
    }
});
export default userListStyles;