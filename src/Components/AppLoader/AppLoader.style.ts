import { StyleSheet } from "react-native";
const appLoaderStyle = StyleSheet.create({
    container:{
        flex :1,
        backgroundColor:'transparent',
        justifyContent:'center',
        alignItems:'stretch'
    }
})
export default appLoaderStyle;